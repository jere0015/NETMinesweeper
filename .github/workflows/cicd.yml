name: CICD

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        - name: "Print1"
          run: |
            ls -la

        - name: "Restore"
          run: dotnet restore

        - name: "Build"
          run: dotnet build --no-restore

        - name: "Print2"
          run: ls -la

        - name: "Test"
          run: dotnet test

